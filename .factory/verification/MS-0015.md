# MS-0015 — Independent verification

Status: **PASS**  
Verifier: mention-scout-verify (independent)  
Date: 2026-08-21  
Branch: `openclaw/ms-0014`  
Worktree: `/home/candr/.openclaw/factory-worktrees/mention-scout/ms-0014`  
PR: https://github.com/rpgrimm/mention-scout/pull/24  
Approved specs:
- `.factory/specs/MS-0015-approved.md`
- `.factory/specs/MS-0014-approved.md` (stacked; still covered)
Implementation notes: `.factory/tasks/MS-0015-implementation.md`  
HEAD reviewed: `4661225` (MS-0015) on top of MS-0014 `75cf6a5` / impl `73074c4`

## Verdict

**PASS** — MS-0015 freeze criteria met; MS-0014 audit/add behavior remains intact. Factory tests green. No merge/ship performed. No code repairs during verification.

## Scope reviewed

- Diff `main...HEAD` (MS-0014 + MS-0015 stack)
- Commit `4661225` MS-0015 delta (code/tests/README/example/specs/tasks)
- `kalshi_mention_scout.py`: `CalendarPhrase.where_to_watch`, parse/load/serialize/add/update, `resolve_calendar_match_options`, `build_calendar_event_body`, CLI `--where-to-watch`, watch-child argv strip, audit schema reminder
- `tests/test_calendar_add.py`, `tests/test_calendar_matches_cli.py`
- `deploy/config/calendar-matches.example.json`, README
- Symlink target, VERSION, no trading / no real SMTP / no existing-event patch

## Evidence map (MS-0015 freeze)

| # | Criterion | Result | Evidence |
|---|-----------|--------|----------|
| 1 | `where_to_watch` on phrase objects; plain strings still valid; schema `version` stays 1 | PASS | `CalendarPhrase.where_to_watch`; `load_calendar_match_config` accepts plain strings + objects; `CALENDAR_MATCH_FILE_VERSION = 1`; serialize keeps version 1; unit + spot-check |
| 2 | CLI `--where-to-watch` with `--add-calendar-match`; casefold update; preserve unspecified fields | PASS | parser dest `where_to_watch`; main rejects orphan flag; `add_calendar_match` update path preserves time/duration/where when unspecified; tests `test_add_where_to_watch_*`, `test_add_where_preserves_unspecified_on_time_update`; spot-check casefold `PLAIN SHOW` |
| 3 | New calendar description includes `Where to watch: …` when resolved; first matched phrase wins | PASS | `build_calendar_event_body` appends line; `resolve_calendar_match_options` first non-empty where wins; tests `test_build_calendar_event_body_includes_where_to_watch`, `test_resolve_calendar_match_options_where_to_watch_first_hit`; spot-check |
| 4 | Not used in match haystack | PASS | Matching uses `phrase_matches()` (match text only) + `event_calendar_haystack` (event fields); where is config metadata only. Spot-check: haystack containing `5-1 nbc` does not hit phrase whose only channel text is `where_to_watch` |
| 5 | No patch of existing Google events required / performed | PASS | `maybe_add_calendar_event_for_new_market` calls `insert_event` only; no `events.patch` / `update_event` API usage in module |
| 6 | VERSION `16.3.0`; symlink still `kalshi_mention_scout.py` | PASS | `VERSION = "16.3.0"`; `./mention_scout.py --version` → `16.3.0`; `mention_scout.py -> kalshi_mention_scout.py` |
| 7 | No trading; tests mock SMTP; factory-test green | PASS | No order/trade placement APIs; audit tests patch `run_swaks_email`; `./scripts/factory-test.sh` → **147 passed** |
| 8 | MS-0014 audit/add still intact | PASS | Prior MS-0014 verification PASS retained on branch; audit/add helpers and tests still present; audit FAIL subject unchanged; schema reminder now also mentions `where_to_watch`; audit OK with where-bearing file (spot-check); CLI suite including MS-0014 cases green |

## Object / string serialization (freeze item 8)

- Plain string only when none of `time`, `duration_minutes`, `where_to_watch` set (`calendar_phrase_to_payload`)
- Any of those fields forces object form
- Confirmed by unit tests + dry-run CLI: `--where-to-watch` alone → object entry

## Empty / validation

- `parse_calendar_where_to_watch` trims and rejects empty → RuntimeError
- Loader rejects blank `where_to_watch` (`test_load_calendar_match_config_rejects_empty_where`)

## Watch child argv

- `_watch_child_arguments` strips `--where-to-watch` (and MS-0014 audit/add flags)
- `test_watch_child_strips_where_to_watch` passes

## Docs / example

- README documents optional `where_to_watch`, description line, first-hit, not used for matching, CLI example
- `deploy/config/calendar-matches.example.json` includes `"where_to_watch": "5-1 nbc"`

## Tests run

```text
./scripts/factory-test.sh
# Compile + CLI --help/--version smoke (mention_scout.py + kalshi_mention_scout.py)
# pytest: 147 passed in 0.23s

.venv/bin/python -m pytest tests/test_calendar_matches_cli.py tests/test_calendar_add.py -q
# 63 passed in 0.09s

.venv/bin/python -m pytest tests/test_calendar_matches_cli.py tests/test_calendar_add.py -q -k "where"
# 8 passed, 55 deselected
```

### Additional verifier spot-checks (not product tests)

- Plain-string load + version 1 round-trip
- Add where → update time preserves where → match-only `already-present` (mtime stable)
- First matched phrase with where wins (`gamma` before `beta`)
- Description contains exact `Where to watch: 5-1 nbc`
- where text alone does not create calendar phrase match
- Empty where rejected
- VERSION 16.3.0; insert-only path; no event patch API
- `--audit-calendar-matches` OK on file with where; FAIL email schema reminder includes `where_to_watch`
- CLI: orphan `--where-to-watch` rejected; dry-run create with where prints object entry and does not write

## MS-0014 stacked coverage note

MS-0014 remains implemented and previously verified PASS (`75cf6a5` / `.factory/verification/MS-0014.md`). This run re-confirmed via full factory suite and targeted audit/add paths under the MS-0015 stack (where field additive; audit subject and add mutual-exclusion preserved).

## Gaps / residual risks

1. **No live Google Calendar insert** in CI — description line covered by unit `build_calendar_event_body` only; owner may smoke one real insert before ship.
2. **Existing calendar rows** intentionally unchanged (freeze); old events will not gain where-to-watch text unless recreated.
3. **Concurrent editors** of `calendar-matches.json`: last-writer-wins atomic replace only (same MS-0014 residual).
4. **Invite + add messaging order** residual from MS-0014 still applies (fails earlier on invite requires calendar-add); still no write.

## Out of scope confirmed

- File-level default `where_to_watch`: not present
- Structured channel parse: free string only
- Separate `--update-calendar-match` flag: not added (update via add path)
- MS-0012 watch-startup FAIL mail: still not implemented (correct)
- No merge, no symlink retarget, no real email, no trading

## Ship blockers for owner

None for verification. Before ship/merge (owner only):

1. Optional manual: `--add-calendar-match … --where-to-watch '5-1 nbc'` then confirm file + one new calendar insert description.
2. Explicit owner ship approval (verification PASS alone is not ship).
3. PR #24 still holds the MS-0014+MS-0015 stack.

## Final

**PASS**
