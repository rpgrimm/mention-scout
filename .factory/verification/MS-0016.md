# MS-0016 — Independent verification

Status: **PASS**  
Verifier: mention-scout-verify (independent)  
Date: 2026-09-25  
Branch: `openclaw/ms-0016`  
Worktree: `/home/candr/.openclaw/factory-worktrees/mention-scout/ms-0016`  
PR: https://github.com/rpgrimm/mention-scout/pull/25 (OPEN, base `main`, not merged)  
Approved spec: `.factory/specs/MS-0016-approved.md`  
Implementation notes: `.factory/tasks/MS-0016-implementation.md`  
HEAD reviewed: `18e309d` (impl `b818523` + PR-record `18e309d`)

## Verdict

**PASS** — implementation matches the approved spec. Factory tests green. Public GET-only. `mention_scout.py` unchanged vs `main`. No merge/ship performed. No trading. No email. No code repairs during verification.

## Scope reviewed

- Diff `main...HEAD` (7 files; new util/tests/docs/factory only)
- `mention_markets.py` CLI, JSON list/target, public REST pagination, word extraction, Google query cleaning, dry-run, browser opener
- `tests/test_mention_markets.py` (mocked HTTP + fake opener)
- `scripts/factory-test.sh` (compile + `--help`/`--version` smoke for the new util)
- README “Mention markets util” section
- `.factory/specs/MS-0016-approved.md`, `.factory/tasks/MS-0016.md`, `.factory/tasks/MS-0016-implementation.md`
- `mention_scout.py` symlink + `kalshi_mention_scout.py` identity vs `main`
- Trader reference ideas only (`market_word` / public GET / cursor pagination) — file not imported or vendored

## Diff vs main

```text
.factory/specs/MS-0016-approved.md
.factory/tasks/MS-0016-implementation.md
.factory/tasks/MS-0016.md
README.md
mention_markets.py
scripts/factory-test.sh
tests/test_mention_markets.py
7 files changed, 1164 insertions(+), 1 deletion(-)
```

Empty diff for `mention_scout.py` and `kalshi_mention_scout.py`. Symlink still `mention_scout.py -> kalshi_mention_scout.py`. SHA-256 of `kalshi_mention_scout.py` matches `main`. `./mention_scout.py --version` → `16.3.0`.

## Evidence map (spec / acceptance)

| # | Criterion | Result | Evidence |
|---|-----------|--------|----------|
| 1 | `list` from `calendar-added.json`; unique ticker; latest `added_at_utc` wins; newest first; `EVENT_TICKER<TAB>added_at_utc<TAB>matched_phrase` | PASS | `load_listed_events` / `cmd_list`; tests `test_parse_calendar_keys_unique_latest_wins`, `test_list_formatting_newest_first`; live owner JSON lists `KXWORLDNEWSMENTION-26SEP25` first |
| 2 | Missing/unreadable/invalid JSON: non-zero, stderr, no browser | PASS | `MentionMarketsError` paths; tests missing file + invalid JSON; unknown-ticker and HTTP-error tests assert opener unused |
| 3 | `target` requires listed ticker (case-insensitive; `::suffix` stripped); else exit 1 and tell user to `list` | PASS | `find_event` + `ticker_from_key`; `test_target_unknown_ticker_exits_1`; spot-check `NOT-A-TICKER` → exit 1, no HTTP |
| 4 | Default action `google-news`; unknown action exit 1 with known names | PASS | `KNOWN_ACTIONS = ("google-news",)`; `test_unknown_action_exits_1` (`trade` refused, opener unused) |
| 5 | Google query cleaning: slashes → spaces; strip trailing `(N+ times)` / `(N times)`; `{cleaned} news` | PASS | `google_news_query`; parametrize table (Starbucks, `OpenAI / Anthropic`, `AI / Artificial Intelligence`, `Iran (3+ times)` / `(3 times)`); live dry-run `OpenAI / Anthropic` → `q=OpenAI+Anthropic+news`, `Iran (3+ times)` → `q=Iran+news` |
| 6 | Word extraction matches trader `market_word` idea (slash subtitle preferred, else `custom_strike["Word"]` / subtitle / ticker) | PASS | `market_word` compared to `kalshi_broadcast_word_trader.market_word`; `test_market_word_prefers_slash_subtitle`; no import/vendor of trader file |
| 7 | Public unauthenticated GET `https://api.elections.kalshi.com/trade-api/v2/markets?event_ticker=…&limit=1000` + cursor pagination (cap 20; fail on repeat cursor) | PASS | `KALSHI_MARKETS_URL` + `method="GET"`; headers `Accept`/`User-Agent` only; FakeUrlOpen asserts host/path; verifier capture: `GET` + `event_ticker=KXWORLDNEWSMENTION-26SEP25&limit=1000`; no `Authorization` |
| 8 | Keep `active`/`open` only; empty usable list exit 1, no browser | PASS | `is_open_market`; tests inactive/unopened skipped; empty open list exit 1, opener unused |
| 9 | `--dry-run` prints `word<TAB>url`, stderr count, **never** opens a browser | PASS | `cmd_target` returns before `open_news_tabs`; `test_dry_run_never_calls_opener`; live CLI dry-run `dry-run: 15 tabs`; exploding opener not called |
| 10 | Browser via stdlib `webbrowser.open_new_tab`; injectable opener; delay between tabs; headless warning still attempts; one false open warns and continues | PASS | `open_news_tabs` / `looks_headless`; paginated test opener called once per market (3 URLs) with `--sleep 0.15`; dry-run skips headless warning |
| 11 | No trading, secrets, calendar JSON writes, extra actions, or `mention_scout.py` behavior change | PASS | GET-only; no POST/PUT/DELETE; no API keys / `.google-password`; no `write_text`/`write_bytes`; scout files identical to `main` |
| 12 | README + spec/task in PR; util version `1.0.0`; `--help`/`--version` | PASS | README section + layout row; `--version` → `mention_markets.py 1.0.0`; factory-test smoke |
| 13 | Offline tests hooked through `./scripts/factory-test.sh` | PASS | new compile/smoke for `mention_markets.py`; pytest collects `tests/test_mention_markets.py` |
| 14 | PR into `main` for desktop test; not merged | PASS | PR #25 `state=OPEN`, `baseRefName=main`, `headRefName=openclaw/ms-0016` |

## Tests run

```text
./scripts/factory-test.sh
# Compile: kalshi_mention_scout.py + mention_markets.py
# CLI smoke: mention_scout.py / kalshi_mention_scout.py / mention_markets.py --help --version
# pytest: 167 passed in 0.24s
# Factory checks passed.
```

20 new tests vs prior MS-0015 baseline of 147 (including 6 query-cleaning parametrize cases). Required spec cases covered: latest-wins parse; list format/missing file; unknown ticker exit 1; query cleaning table; paginated `/markets` mock → words/URLs/opener-once-per-market; `--dry-run` never calls opener; inactive/unopened not opened.

### Additional verifier spot-checks (not product tests)

- Owner `~/.config/mention-scout/calendar-added.json` v1, 26 entries; `./mention_markets.py list` first line `KXWORLDNEWSMENTION-26SEP25	2026-09-25T17:29:37.185609Z	abc world news tonight`
- `./mention_markets.py target KXWORLDNEWSMENTION-26SEP25 --dry-run` against live public REST: 15 tabs; includes `OpenAI / Anthropic` → `https://www.google.com/search?q=OpenAI+Anthropic+news`
- Same dry-run with exploding opener: exit 0, opener never called
- Unknown ticker: exit 1, `urlopen` not called
- Constructed request: `GET https://api.elections.kalshi.com/trade-api/v2/markets?event_ticker=KXWORLDNEWSMENTION-26SEP25&limit=1000` (no auth header)
- `mention_scout.py` / `kalshi_mention_scout.py` SHA-256 identical to `main`; version `16.3.0`
- Scan: no `kalshi_broadcast` import, no `requests`, no POST, no `google-password`

## Code review notes

- Standalone stdlib util; does not grow `kalshi_mention_scout.py` (correct per spec).
- Pagination/retry/timeout constants match spec spirit (20s timeout, a few retries, 20-page cap, repeated-cursor fail).
- `target` always prints `word<TAB>url` (including when opening tabs). Spec requires that for `--dry-run`; extra stdout when opening is inspectable and not a fail.
- `--timeout` / `--retries` are internal, not CLI flags (implementation note; not required by spec).
- Dedup is per market ticker; sort is case-insensitive word then ticker (one tab per market, not per slash-alias).

## Gaps / residual risks

1. **Owner desktop tab-open** (`target` without `--dry-run`) not exercised here — this host is verification/staging; opener behavior is covered by injected tests. Owner still needs to click through on a display.
2. **Retry / repeated-cursor / max-pages** paths are implemented but not unit-tested. Live dry-run succeeded with a normal payload (15 markets).
3. **HTTP 429/5xx retry** is present; only 404 no-browser is tested.

None of these are ship blockers for verification PASS.

## Out of scope confirmed

- No trading / orders / WebSockets / API keys / kalshi-python-sync
- No import or vendor of `kalshi_broadcast_word_trader.py`
- No extra actions beyond `google-news`
- No direct Kalshi-only target when ticker is absent from JSON
- No calendar JSON writes
- No merge to `main`, no release, no systemd, no email

## Ship blockers for owner

None for verification. Before ship/merge (owner only):

1. Desktop proof: `list`, then `target … --dry-run`, then `target` without `--dry-run` (one Google tab per active/open market).
2. Explicit owner ship approval (verification PASS alone is not ship).
3. PR #25 remains the review publication; do not merge until owner says so.

## Final

**PASS**
