# MS-0014 — Independent verification

Status: **PASS**  
Verifier: mention-scout-verify (independent)  
Date: 2026-08-21  
Branch: `openclaw/ms-0014`  
Worktree: `/home/candr/.openclaw/factory-worktrees/mention-scout/ms-0014`  
PR: https://github.com/rpgrimm/mention-scout/pull/24  
Issue: https://github.com/rpgrimm/mention-scout/issues/23  
Approved spec: `.factory/specs/MS-0014-approved.md`  
Implementation notes: `.factory/tasks/MS-0014-implementation.md`  
HEAD reviewed: `b1ca232` (implementation `73074c4` + report)

## Verdict

**PASS** — implementation matches the owner freeze and normative requirements. Factory tests green. No merge/ship performed.

## Scope reviewed

- Diff `main...HEAD` (8 files; code/docs/tests/factory only)
- `kalshi_mention_scout.py` helpers + CLI/main routing + watch child argv strip
- `tests/test_calendar_matches_cli.py`
- README calendar section (audit/add + MS-0012 boundary)
- Symlink target, VERSION, absence of MS-0012 / trading APIs

## Evidence map (acceptance / freeze)

| Criterion | Result | Evidence |
|-----------|--------|----------|
| `--audit-calendar-matches` one-shot; same loader rules | PASS | `run_audit_calendar_matches` → `load_calendar_match_config`; tests `test_audit_ok_no_email`, bad JSON/schema/missing |
| Audit success: exit 0, stdout summary, no email | PASS | stdout `calendar-matches audit ok` + path/version/phrases/default_time; `run_swaks_email` not called |
| Audit fail: exit 1, stderr; FAIL email default ON | PASS | return 1; `email_on_fail` default True; subject exact `[Kalshi] FAIL | calendar-matches audit` |
| `--no-email-on-fail` / `--email-on-fail` | PASS | mutually exclusive argparse group; tests no-email + default email path |
| Audit success never emails | PASS | success path has no swaks call |
| `--add-calendar-match` + required `--match`; optional `--time` / `--duration-minutes` / `--dry-run` | PASS | parser + `run_add_calendar_match` / `add_calendar_match`; main requires `--match` |
| Missing file: create v1; no invented `default_time` | PASS | `created-file` payload `{"version":1,"phrases":[...]}` only |
| Invalid existing: refuse write, no clobber | PASS | `refusing to modify invalid…`; original bytes unchanged (unit + spot) |
| Atomic write mode 600 | PASS | `save_calendar_match_config` → `write_cache_atomic`; test asserts `0o600` |
| Preserve phrases + `default_time`; order preserved | PASS | `test_add_preserves_order_and_default_time`, object round-trip |
| Casefold duplicate: update when time/duration; else already-present | PASS | `test_add_duplicate_casefold_update`, `test_add_already_present_no_write` (mtime stable) |
| Plain string vs object rules | PASS | string when neither schedule field; object when either; duration-only object confirmed in spot-check |
| Update preserves unspecified fields | PASS | time-only update keeps duration; duration-only update keeps time |
| Canonical rewrite known keys only | PASS | spot-check drops `extra`, keeps `default_time` + phrases |
| Mutual exclusion vs watch/email/calendar-add/queue/test-email/calendar-auth/invite/each other | PASS | main oneshot_conflicts + orphan flag checks; tests + spot-check (see residual) |
| Watch child argv strip | PASS | `_watch_child_arguments` strips audit/add/match/time/duration/dry-run/email-on-fail flags; unit test |
| No trading | PASS | no order/trade APIs in diff or module scan |
| No real SMTP in tests | PASS | all mail paths mock `run_swaks_email` / `verify_email_configuration` |
| Secrets not in mail body | PASS | password not in body; formatter paths/error/schema only |
| Symlink not retargeted | PASS | `mention_scout.py` → `kalshi_mention_scout.py` |
| MS-0012 not implemented | PASS | no `abort_watch_startup` / start-FAIL subject implementation |
| README docs + MS-0012 boundary + optional cron | PASS | Validate / Add subsections under calendar matches |
| VERSION bump | PASS | `16.1.0` → `16.2.0` (minor; new user-facing modes) |
| Exit codes 0/1 | PASS | audit/add return 0/1; flag errors `SystemExit` |

## Tests run

```text
./scripts/factory-test.sh
# Compile + CLI --help/--version smoke (mention_scout.py + kalshi_mention_scout.py)
# pytest: 139 passed in 0.22s

.venv/bin/python -m pytest tests/test_calendar_matches_cli.py -q
# 24 passed in 0.06s
```

Additional verifier spot-checks (not committed as product tests): empty match rejection; invalid `--time` leaves file unchanged; duration-only object form; duration update preserves time; unknown top-level key dropped on rewrite; additional mutual-exclusion argv cases; subject/VERSION/symlink/MS-0012/trading scans.

## Code review notes

- Audit email body matches freeze intent (timestamp, host, version, path, truncated error, schema reminder, remediation, MS-0014 vs MS-0012 note).
- Add path reuses `parse_calendar_local_time` / `parse_calendar_duration_minutes` and loader for existing files.
- Parent config dir `chmod 0700` best-effort on create; atomic replace via existing cache writer.
- Dry-run prints `calendar-matches dry-run` + `dry-run: not written` (documented implementation deviation; still no write, exit 0).
- Help text for `--add-calendar-match` is slightly awkward (“Optional is atomic…”) — cosmetic only, not a functional fail.

## Gaps / residual risks

1. **Invite + add messaging order:** `--add-calendar-match --match x --invite-email a@b.c` fails fast on the earlier `--invite-email requires --calendar-add-new` check before the MS-0014 oneshot conflict list. Still refused with no write/mail; message differs from pure “one-shot cannot be combined with --invite-email.” Acceptable residual UX nit.
2. **Owner manual proof not done here:** real SMTP FAIL email receipt, live config path round-trip, and watch reload after add remain owner ship-checklist items (explicit non-CI in spec).
3. **Concurrent editors:** last-writer-wins atomic replace only (spec v1; documented risk).
4. **Canonical rewrite** drops unknown top-level keys (frozen/documented).
5. **No automated test** that a mid-write crash leaves the previous good file (helper reuses battle-tested `write_cache_atomic`; mode/round-trip covered).

## Out of scope confirmed

- MS-0012 watch-startup FAIL mail not present (correct).
- No symlink retarget, no merge, no real email, no trading.
- No CRUD beyond add/audit; no CLI `default_time` setter.

## Ship blockers for owner

None for verification. Before ship/merge (owner only):

1. Optional manual: break match file → audit → confirm FAIL email; fix → audit ok.
2. Optional manual: `--add-calendar-match` then confirm watch/calendar load.
3. Explicit owner ship approval (verification PASS alone is not ship).

## Final

**PASS**
