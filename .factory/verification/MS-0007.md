# MS-0007 — Independent verification

Status: **PASS**  
Verifier role: mention-scout-verify (independent; did not implement)  
Date: 2026-08-01  
Worktree: `/home/candr/.openclaw/factory-worktrees/mention-scout/ms-0007`  
Branch: `openclaw/ms-0007`  
Commit verified: `0a42604f2d8603a97abade764982fca18deef0c7` (HEAD confirmed)  
Approved spec: `.factory/specs/MS-0007-approved.md`  
Implementation notes: `.factory/tasks/MS-0007-implementation.md`  
GitHub: https://github.com/rpgrimm/mention-scout/issues/9  

---

## Scope of review

- Read approved spec acceptance criteria and normative requirements R1–R10.
- Inspected full product diff vs `main` (`kalshi_mention_scout.py` + new `tests/test_mention_gate.py`).
- Confirmed gate helpers, regex immutability, MS-0006 non-touch, cache/CLI/trading/email posture.
- Ran offline compile / CLI smoke / full pytest with healthy interpreter (worktree `.venv` is broken ensurepip stub).
- Manual fixture admission boolean spot-check on `KXGENERICTARIFF-26JUL`.

No product code was modified during verification. No real email, trading, merge, or ship approval.

---

## Diff summary (product-relevant)

```
kalshi_mention_scout.py  | + helper split / is_mention_market retarget / scan admission helper
tests/test_mention_gate.py | +163 (new)
factory docs/fixture     | spec + task + fixture (non-runtime)
```

Core code change (Option A as approved):

| Surface | Behavior |
|---------|----------|
| `text_for_market` | Still includes `rules_primary` / `rules_secondary` (contains/display) |
| `mention_gate_text_for_market` | **New** — product fields only (no rules) |
| `is_mention_market` | Uses gate text; ticker `MENTION` fast-path unchanged |
| `event_qualifies_as_mention` | **New** pure scan-admission boolean |
| `scan_mention_events` | Admission via `event_qualifies_as_mention` |
| `scan_paused_mention_markets` / final `main()` filter | Still call `is_mention_market` (inherits fix) |
| `contains_filter` | Still uses full `text_for_market` |
| `MENTION_RE` / `SAY_EVENT_RE` | Byte-identical to `main` |
| `CACHE_FORMAT_VERSION` / `QUEUE_FORMAT_VERSION` / `VERSION` | Unchanged (`5` / `1` / `16.0.0`) |
| MS-0006 type layer | No symbol/diff changes |
| CLI argparse defaults | No diff |
| Trading / SMTP paths | Not introduced |

Stable entry: `mention_scout.py` → `kalshi_mention_scout.py` (symlink preserved).

---

## Acceptance criteria

| Criterion | Result | Evidence |
|-----------|--------|----------|
| `is_mention_market` no longer matches solely due to rules text | **PASS** | `is_mention_market` searches `mention_gate_text_for_market` only; fixture nested market → False while `MENTION_RE` still hits `rules_secondary` / full `text_for_market` |
| Fixture parent false, market false, scan-admission false | **PASS** | Manual + `tests/test_mention_gate.py::test_ms0007_fixture_rejected_by_binary_gates` |
| True-positive ticker-MENTION / title mention / title say without rules | **PASS** | parametrized ticker cases + title say/mention tests in `test_mention_gate.py` |
| `contains_filter` still searches rules | **PASS** | `test_contains_still_searches_rules_while_gate_rejects` (`ZYXCONTAINSRULES`); manual `contains_filter(..., "incidentally")` True on fixture |
| `tests/test_mention_types.py` (MS-0006) still passes, intent unchanged | **PASS** | 44 type tests green; no type-layer diff vs main |
| No cache/queue format bump; no CLI default change; no trading; no real SMTP in CI | **PASS** | Diff + constants inspection + offline tests only |
| Factory test suite equivalent green | **PASS** | See commands below (52 passed). Note: `./scripts/factory-test.sh` prefers broken local `.venv` and would false-fail on missing pytest unless interpreter overridden/stub removed — product code OK |
| Independent verification PASS (this document) | **PASS** | This record. Owner ship approval is a separate gate (not granted here) |

---

## Normative requirements R1–R10

| Req | Verdict | Notes |
|-----|---------|-------|
| **R1** Rules must not drive binary admission | **PASS** | Gate text excludes `rules_primary` / `rules_secondary`; rules-only hit → False |
| **R2** Non-rules product fields remain gate inputs | **PASS** | Gate fields: ticker, event_ticker, title, subtitle, yes_sub_title, no_sub_title + MENTION fast-path + both regexes; parent `event_text_for_match` unchanged |
| **R3** Fixture rejection | **PASS** | `is_mention_event` False; `is_mention_market` False; admission boolean False for `KXGENERICTARIFF-26JUL` / nested `…-NOV03` |
| **R4** True positives preserved | **PASS** | Ticker MENTION, title say, title mention tests with empty/irrelevant rules |
| **R5** `--contains` may still search rules | **PASS** | `contains_filter` → `text_for_market`; docstring/comment documents split |
| **R6** Scan + final filter consistency | **PASS** | Compact scan + paused scan + `main()` final filter all use corrected `is_mention_market` / shared admission helper; no alternate rules-inclusive membership path found |
| **R7** Watch/email/queue inherit fix | **PASS** | No separate watch classifier; watch path uses mention inventory after same filters. No real email exercised |
| **R8** MS-0006 intact | **PASS** | Type registry/priority/subjects/`--type` untouched; type tests green |
| **R9** Compatibility / guardrails | **PASS** | No trading; no real SMTP; symlink preserved; no deliberate CLI/cache/queue bumps; fixture is public metadata |
| **R10** Failure behavior | **PASS** | Missing fields → empty strings / False (unit test); stale cache dropped by final `is_mention_market`; contains-on-rules without admission covered |

---

## Commands and results

Interpreter used (worktree `.venv` broken):

```bash
MS6_PY=/home/candr/.openclaw/factory-worktrees/mention-scout/ms-0006/.venv/bin/python
cd /home/candr/.openclaw/factory-worktrees/mention-scout/ms-0007
git rev-parse HEAD
# 0a42604f2d8603a97abade764982fca18deef0c7

"$MS6_PY" -m py_compile kalshi_mention_scout.py
# COMPILE_OK

"$MS6_PY" ./mention_scout.py --version
# mention_scout.py 16.0.0
"$MS6_PY" ./kalshi_mention_scout.py --version
# kalshi_mention_scout.py 16.0.0
# --help smokes OK for both entries

"$MS6_PY" -m pytest -q
# 52 passed in 0.04s

"$MS6_PY" -m pytest -q tests/test_mention_gate.py tests/test_mention_types.py -v --tb=no
# test_mention_gate.py ........ (8)
# test_mention_types.py .... (44)
# 52 passed
```

### Manual fixture spot-check

```
event_ticker KXGENERICTARIFF-26JUL
market_ticker KXGENERICTARIFF-26JUL-NOV03
is_mention_event False
is_mention_market False
event_qualifies_as_mention False
admission_boolean False
rules has mention True
gate has rules? False
full text has mention True
gate text has mention False
contains incidentally True
```

---

## Gaps / residual risks (accepted or non-blocking)

1. **Worktree `.venv` broken** — `./scripts/factory-test.sh` auto-selects `.venv/bin/python` when executable, so a naive script run fails with `pytest is not installed` even though product tests pass under another interpreter. Not a product defect; environment hygiene for implementer/coordinator.
2. **True market whose only product signal lives exclusively in rules** — explicitly accepted by owner in approved spec.
3. **Future boilerplate in titles** — out of scope; would need a new MS.
4. **Stale compact caches** may still list pre-fix false positives until TTL/`--refresh`; final filter correctly drops them (R6/R10).
5. **No live `--watch-new` smoke** — not required by spec (offline unit coverage sufficient).
6. **Ship gate** — independent verification PASS does **not** constitute owner release approval or merge authorization.

---

## Verdict

**PASS**

All approved acceptance criteria and R1–R10 are met on commit `0a42604f2d8603a97abade764982fca18deef0c7`. Tests green (52). Ready for owner ship decision; do not merge/ship from this verification alone.

---

*End of MS-0007 independent verification.*
