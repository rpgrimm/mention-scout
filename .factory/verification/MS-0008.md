# MS-0008 Independent Verification

Status: **PASS**  
Date: 2026-08-01  
Verifier: mention-scout-verify (independent)  
Worktree: `/home/candr/.openclaw/factory-worktrees/mention-scout/ms-0008`  
Branch: `openclaw/ms-0008`  
HEAD verified: `236488e346e74183d637935d58ee1157aad97f1a`  
Commit: `MS-0008 add systemd user unit template and install helper (#10)`  
Issue: https://github.com/rpgrimm/mention-scout/issues/10  
Approved spec: `.factory/specs/MS-0008-approved.md`

## Verdict

**PASS** — Packaging-only systemd user unit template + install helper + README ops section + offline installer tests match the approved MS-0008 defaults. Factory tests pass (54). No product/discovery/classification changes. No real email/SMTP. No secrets in unit or dry-run output. Dry-run does not write the unit; temp `XDG_CONFIG_HOME` install/uninstall works with stub `systemctl`.

**Ship recommendation:** ready for owner ship approval? **yes**  
(Do **not** merge/ship from this report alone — owner ship approval still required.)

## Scope reviewed

Diff vs `main` (`6281c46`…`236488e`), 8 files, packaging/docs/tests/factory only:

| Path | Role |
|------|------|
| `deploy/systemd/mention-scout-watch.service` | User unit template with placeholders |
| `scripts/install-user-service.sh` | Install/update/enable/start/disable/uninstall/dry-run helper |
| `README.md` | § “Run as a systemd user service” |
| `tests/test_install_user_service.py` | Offline dry-run + temp XDG install/uninstall |
| `.factory/specs/MS-0008-*.md`, `.factory/tasks/MS-0008*.md` | Spec/task records |

**Not changed:** `kalshi_mention_scout.py`, `mention_scout.py` symlink target behavior, cache format, classification, trading paths. `git diff main...HEAD -- kalshi_mention_scout.py mention_scout.py` is empty (0 bytes).

## Diff / guardrail review

| Check | Result | Evidence |
|-------|--------|----------|
| Packaging only (no discovery/trading) | **PASS** | Diff limited to deploy/scripts/README/tests/factory; no product code |
| User unit only (not system) | **PASS** | Install path `${XDG_CONFIG_HOME:-$HOME/.config}/systemd/user/`; `systemctl --user`; WantedBy=default.target |
| No secrets in template/git | **PASS** | Template has comment-only password reference; no `/home/` hardcodes; no env password embedding |
| Password not read/printed | **PASS** | Preflight uses existence + `stat` mode only; dry-run with fake password content did not leak |
| No real email in verify | **PASS** | Tests/dry-run only; no swaks/SMTP invocation |
| Interactive CLI defaults unchanged | **PASS** | No argparse/product changes; README states bare `--watch-new` stays watch-only |
| Stable entry preserved | **PASS** | Symlink `mention_scout.py -> kalshi_mention_scout.py`; installer uses stable entry path (not resolved impl name) |
| Commit scope matches MS-0008 | **PASS** | Message and file list are packaging/install UX for #10 |

## Acceptance criteria map

| Criterion | Result | Evidence |
|-----------|--------|----------|
| Unit template committed under approved path with placeholders only | **PASS** | `deploy/systemd/mention-scout-watch.service` with `@REPO_ROOT@`, `@PYTHON@`, `@ENTRY@`; no host-only abs paths |
| Installer installs user unit to `~/.config/systemd/user/` (or XDG) | **PASS** | Script + temp XDG write smoke + pytest `test_install_to_temp_xdg_without_systemctl` |
| Default ExecStart: stable entry + `--watch-new --email-new` | **PASS** | Template default + dry-run ExecStart with absolute python + `…/mention_scout.py --watch-new --email-new` |
| `--no-email` installs watch-only | **PASS** | Dry-run and temp install omit `--email-new` from ExecStart |
| Supports dry-run, enable/start, disable, uninstall | **PASS** | Flags present; dry-run exercised; uninstall removes unit under temp XDG; enable/start path present (`--enable`, `--now`/`--start`, `--enable-now`) |
| Re-install idempotent enough to update unit safely | **PASS** | Install path always re-renders and atomically replaces unit file |
| No secrets in template, doc examples, or git | **PASS** | Hygiene checks + grep; docs reference path only |
| README: linger, journalctl, prereqs, WorkingDirectory/cache, uninstall | **PASS** | README §125–202 covers all; ops caution for unattended email noise / prefer `--no-email` (substance of MS-0007 caution; does not hard-name MS-0007/#9) |
| Interactive CLI + discovery unchanged when installer unused | **PASS** | No product diff; factory CLI smoke still passes |
| Restart on-failure with delay/start limits (no tight loop) | **PASS** | `Restart=on-failure`, `RestartSec=30`, `StartLimitIntervalSec=300`, `StartLimitBurst=5` |
| Offline checks: template, dry-run, factory-test; no real email | **PASS** | See commands below; 54 pytest passed |
| Independent verification PASS + owner ship before release | **PASS** (this report); owner ship still pending | |

## Normative requirements (R1–R7)

| Req | Result | Notes |
|-----|--------|-------|
| R1 Template | **PASS** | Path, placeholders, restart/journal/NoNewPrivileges, no secrets |
| R2 Installer | **PASS** | Executable; abs REPO_ROOT/PYTHON/ENTRY; XDG unit dir; daemon-reload; lifecycle flags; default email-on; `--no-email`; non-zero on hard failures |
| R3 No secrets | **PASS** | No embedded passwords/tokens; preflight never prints contents |
| R4 CLI unchanged | **PASS** | No product code change; RestartSec=30 ≥ 15 |
| R5 Docs | **PASS** | Full user-service section; linger manual by default; journalctl; cache/workdir; uninstall; email noise caution |
| R6 Safety | **PASS** | No real email; offline verify; user units only |
| R7 CLI wrapper | **PASS** (deferred) | No `--install-user-service` added |

## Commands run (verifier)

```text
git rev-parse HEAD
# 236488e346e74183d637935d58ee1157aad97f1a

git log -1 --oneline
# 236488e MS-0008 add systemd user unit template and install helper (#10)

git diff --stat main...HEAD
# 8 files, packaging/docs/tests/factory only

git diff main...HEAD -- kalshi_mention_scout.py mention_scout.py
# empty

./scripts/factory-test.sh
# Compile + help/version smoke + pytest
# 54 passed in 0.14s
# Factory checks passed.

./scripts/install-user-service.sh --dry-run
# exit 0; absolute WorkingDirectory/ExecStart; --watch-new --email-new;
# would write …; no unit file created under real ~/.config

./scripts/install-user-service.sh --dry-run --no-email
# exit 0; ExecStart watch-only

# Temp XDG install + uninstall with stub systemctl --user
# install exit 0; unit written under $XDG_CONFIG_HOME/systemd/user/
# ExecStart … --watch-new (no email); stub logged daemon-reload
# uninstall exit 0; unit removed

# HOME with fake password file + dry-run: password contents not in output
test -x scripts/install-user-service.sh  # executable_ok
```

## Spec criteria checklist (condensed)

- [x] Template at `deploy/systemd/mention-scout-watch.service`, placeholders only
- [x] Installer executable; dry-run / install / enable-start options / `--no-email` / uninstall/disable
- [x] Default ExecStart includes `--watch-new --email-new`; `--no-email` drops email
- [x] WorkingDirectory / abs python + stable entry from repo root
- [x] No classification/trading/discovery logic changes
- [x] README: linger (not auto), journalctl, uninstall, email prereqs, email-noise caution
- [x] `./scripts/factory-test.sh` passes (54)
- [x] Dry-run does not write unit; temp XDG install/uninstall covered
- [x] No real email; password existence/mode only
- [x] Commit message/scope matches MS-0008

## Residual risks

1. **Unattended email noise (MS-0007 / #9 still open):** default unit enables `--email-new`. Mitigated by README ops caution + installer `--no-email`; not a packaging blocker per approved spec.
2. **Live `systemctl --user` bus not exercised end-to-end here:** offline stub + dry-run only. First real enable/start on owner machine remains a manual smoke.
3. **Linger / PATH under pure boot:** documented; installer hints when linger off; optional explicit `--enable-linger` exists (opt-in, not default). `swaks` may still be missing from minimal user-unit PATH — docs/preflight warn.
4. **Checkout move:** unit keeps absolute paths until reinstall — documented.
5. **README ops caution** describes classification noise and `--no-email` but does not literally cite “MS-0007” / “#9”. Substance meets R5; minor doc nit only.

## Explicit non-actions

- Did **not** modify product code
- Did **not** merge or ship
- Did **not** send email, enable linger in automation, or install a system unit
- Did **not** start a long live Kalshi watch

## Ship recommendation

| Question | Answer |
|----------|--------|
| Independent verification | **PASS** |
| Ready for owner ship approval? | **yes** |
| Merge/ship now without owner? | **no** |
